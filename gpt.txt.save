// Verifica se a pergunta começa com "quanto" ou "quantos"
if (strcmp(questionList(pergunta), "quanto") == 0 || 
strcmp(questionList(pergunta), "quantos") == 0) {
    
    // Verifica se a pergunta fala sobre "títulos" (Ex.: "Quantos títulos 
de campeão brasileiro o Flamengo já conquistou?")
    if (strstr(pergunta, "títulos") != NULL) {
        printf(""); // Aqui você pode colocar a resposta.
    } 
    // Verifica se a pergunta fala sobre "gols" (Ex.: "Quantos gols o 
Flamengo marcou na final da Libertadores de 2019?")
    else if (strstr(pergunta, "gols") != NULL) {
        printf(""); 
    } 
    // Verifica se a pergunta fala sobre "placar" (Ex.: "Quanto foi o 
placar da maior goleada do Flamengo em um clássico?")
    else if (strstr(pergunta, "placar") != NULL) {
        printf(""); 
    } 
    // Verifica se a pergunta fala sobre "jogadores" (Ex.: "Quantos 
jogadores do Flamengo já foram convocados para uma Copa do Mundo?")
    else if (strstr(pergunta, "jogadores") != NULL) {
        printf(""); 
    } 
    // Verifica se a pergunta fala sobre "times" (Ex.: "Quantos clubes 
participam da Série A do Campeonato Brasileiro de Futebol?")
    else if (strstr(pergunta, "times") != NULL) {
        printf(""); 
    } 
    // Verifica se a pergunta fala sobre "minutos" (Ex.: "Quantos minutos 
dura uma partida oficial de futebol?")
    else if (strstr(pergunta, "minutos") != NULL) {
        printf(""); 
    } 
    // Verifica se a pergunta fala sobre "cartões" (Ex.: "Quantos cartões 
amarelos resultam em uma suspensão no futebol?")
    else if (strstr(pergunta, "cartões") != NULL) {
        printf(""); 
    } 
    // Verifica se a pergunta fala sobre "árbitros" (Ex.: "Quantos 
árbitros, incluindo assistentes, atuam em uma partida oficial de 
futebol?")
    else if (strstr(pergunta, "árbitros") != NULL) {
        printf(""); 
    } 
    // Verifica se a pergunta fala sobre "substituições" (Ex.: "Quantos 
jogadores podem ser substituídos durante uma partida oficial de futebol?")
    else if (strstr(pergunta, "substituições") != NULL) {
        printf(""); 
    } 
    // Verifica se a pergunta fala sobre "sequência" (Ex.: "Quanto tempo 
durou a maior sequência de vitórias do Flamengo?")
    else if (strstr(pergunta, "sequência") != NULL) {
        printf(""); 
    }
}

